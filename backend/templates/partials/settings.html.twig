{#
    Settings panel — merged view combining admin operations (import, health)
    and configuration (preferences, alerts, system deployment info).

    Sub-section navigation is driven by the browser-local $_settingsSection signal.
    Default section: 'preferences'.
#}
<div class="settings-layout">

    {# ── Vertical sub-nav ─────────────────────────────────────────────────── #}
    <div class="settings-nav">
        <nav class="tabs" role="tablist" aria-label="Settings sections">

            <div class="muted upper" style="font-size: .7rem; letter-spacing: .05em; font-weight: 600;">Operations</div>

            <a href="#" role="tab"
               class="cluster"
               data-class:active="$_settingsSection === 'import'"
               data-on:click__prevent="$_settingsSection = 'import'">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor"  viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M8 3a5 5 0 1 0 4.546 2.914.5.5 0 0 1 .908-.417A6 6 0 1 1 8 2z"/>
                    <path d="M8 4.466V.534a.25.25 0 0 1 .41-.192l2.36 1.966c.12.1.12.284 0 .384L8.41 4.658A.25.25 0 0 1 8 4.466"/>
                </svg>
                Import
                <span class="spacer badge" data-level="warning"
                      data-show="${{ import_running.id() }}" style="display:none;"
                      title="Import in progress">
                    <span class="visually-hidden">running</span>
                </span>
            </a>

            <a href="#" role="tab"
               class="cluster"
               data-class:active="$_settingsSection === 'health'"
               data-on:click__prevent="$_settingsSection = 'health'">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor"  viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M6 2a.5.5 0 0 1 .47.33L10 12.036l1.53-4.208A.5.5 0 0 1 12 7.5h3.5a.5.5 0 0 1 0 1h-3.15l-1.88 5.17a.5.5 0 0 1-.94 0L6 3.964 4.47 8.171A.5.5 0 0 1 4 8.5H.5a.5.5 0 0 1 0-1h3.15l1.88-5.17A.5.5 0 0 1 6 2"/>
                </svg>
                Health
            </a>

            <div class="muted upper" style="font-size: .7rem; letter-spacing: .05em; font-weight: 600;">Configuration</div>

            <a href="#" role="tab"
               class="cluster"
               data-class:active="$_settingsSection === 'preferences'"
               data-on:click__prevent="$_settingsSection = 'preferences'">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor"  viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M11.5 2a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3M9.05 3a2.5 2.5 0 0 1 4.9 0H16v1h-2.05a2.5 2.5 0 0 1-4.9 0H0V3zM4.5 7a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3M2.05 8a2.5 2.5 0 0 1 4.9 0H16v1H6.95a2.5 2.5 0 0 1-4.9 0H0V8zm9.45 4a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3m-2.45 1a2.5 2.5 0 0 1 4.9 0H16v1h-2.05a2.5 2.5 0 0 1-4.9 0H0v-1z"/>
                </svg>
                Preferences
            </a>

            <a href="#" role="tab"
               class="cluster"
               data-class:active="$_settingsSection === 'alerts'"
               data-on:click__prevent="$_settingsSection = 'alerts'">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor"  viewBox="0 0 16 16">
                    <path d="M8 16a2 2 0 0 0 2-2H6a2 2 0 0 0 2 2M8 1.918l-.797.161A4 4 0 0 0 4 6c0 .628-.134 2.197-.459 3.742-.16.767-.376 1.566-.663 2.258h10.244c-.287-.692-.502-1.491-.663-2.258C12.134 8.197 12 6.628 12 6a4 4 0 0 0-3.203-3.92zM14.22 12c.223.447.481.801.78 1H1c.299-.199.557-.553.78-1C2.68 10.2 3 6.88 3 6c0-2.42 1.72-4.44 4.005-4.901a1 1 0 1 1 1.99 0A5 5 0 0 1 13 6c0 .88.32 4.2 1.22 6"/>
                </svg>
                Alerts
            </a>

            <a href="#" role="tab"
               class="cluster"
               data-class:active="$_settingsSection === 'system'"
               data-on:click__prevent="$_settingsSection = 'system'">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor"  viewBox="0 0 16 16">
                    <path d="M14 10a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1zM2 9a2 2 0 0 0-2 2v1a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-1a2 2 0 0 0-2-2z"/>
                    <path d="M5 11.5a.5.5 0 1 1-1 0 .5.5 0 0 1 1 0m-2 0a.5.5 0 1 1-1 0 .5.5 0 0 1 1 0M14 6a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1zM2 5a2 2 0 0 0-2 2v1a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2z"/>
                    <path d="M5 7.5a.5.5 0 1 1-1 0 .5.5 0 0 1 1 0m-2 0a.5.5 0 1 1-1 0 .5.5 0 0 1 1 0M14 2a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V3a1 1 0 0 1 1-1zM2 1a2 2 0 0 0-2 2v1a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2z"/>
                    <path d="M5 3.5a.5.5 0 1 1-1 0 .5.5 0 0 1 1 0m-2 0a.5.5 0 1 1-1 0 .5.5 0 0 1 1 0"/>
                </svg>
                System
            </a>

        </nav>
    </div>

    {# ── Content panels ───────────────────────────────────────────────────── #}
    <div>

        {# ── Import section ───────────────────────────────────────────────── #}
        <div data-show="$_settingsSection == 'import'" style="display:none;">

            {# ── Action buttons ────────────────────────────────────────────── #}
            <div>

            {% if daemonDisabled or daemonsInfo is empty %}

                <p class="muted">
                    Import daemon is disabled (<code>NFSEN_SKIP_DAEMON=1</code>).
                </p>

            {% elseif daemonsInfo|length > 1 %}

                {# ── Multi-profile daemon table ──────────────────────────── #}
                <div class="card">
                    <div class="cluster card-header strong muted cluster-between">
                        Import Daemon
                    </div>
                    <div class="card-body">
                <table class="muted">
                    <thead>
                        <tr>
                            <th>Profile</th>
                            <th>Status</th>
                            <th>Last auto-import</th>
                            <th>Dirs watched</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for profile, info in daemonsInfo %}
                    <tr>
                        <td class="mono">{{ profile }}</td>
                        <td>
                            {% if info.ready %}
                                <span class="badge" data-level="success">ready</span>
                            {% else %}
                                <span class="badge text-warning" data-level="warning">init&hellip;</span>
                            {% endif %}
                        </td>
                        <td>
                            {% if info.lastAutoImport > 0 %}
                                <span data-text="new Date({{ info.lastAutoImport }} * 1000).toLocaleString(undefined, tzOptions(${{ displayTz.id() }}, ${{ nfcapdTz.id() }}))"></span>
                            {% else %}
                                <span class="muted">never</span>
                            {% endif %}
                        </td>
                        <td>{{ info.watchCount }}</td>
                        <td>
                            <div class="cluster">
                                <button data-size="sm" data-variant="primary"
                                        data-attr:disabled="${{ import_running.id() }}"
                                        data-show="!${{ import_running.id() }}"
                                        data-on:click="${{ admin_target_profile.id() }} = '{{ profile }}'; @post('{{ triggerImport.url() }}')">
                                    Trigger
                                </button>
                                {% if datasourceAcceptsHistoricWrites %}
                                <button data-size="sm"
                                        data-attr:disabled="${{ import_running.id() }}"
                                        data-show="!${{ import_running.id() }}"
                                        title="Re-reads every capture file, including those older than the newest sample. Nothing is deleted."
                                        data-on:click="${{ admin_target_profile.id() }} = '{{ profile }}'; @post('{{ backfillImport.url() }}')">
                                    Backfill
                                </button>
                                {% else %}
                                <button class="kill" data-size="sm"
                                        data-attr:disabled="${{ import_running.id() }}"
                                        data-show="!${{ confirm_rescan.id() }} && !${{ import_running.id() }}"
                                        data-on:click="${{ admin_target_profile.id() }} = '{{ profile }}'; ${{ confirm_rescan.id() }} = true">
                                    Rescan
                                </button>
                                {% endif %}
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                    </tbody>
                </table>
                    </div>
                </div>

                {# Shared confirm-rescan dialog (shown after clicking Rescan for any profile) #}
                {% if not datasourceAcceptsHistoricWrites %}
                <div data-show="${{ confirm_rescan.id() }}" style="display: none;" >
                    <div class="cluster">
                        <span class="text-danger strong muted">
                            ⚠ Resets all {{ deployDatasource }} data for
                            <strong><span data-text="${{ admin_target_profile.id() }}"></span></strong>
                            and re-imports from scratch.
                        </span>
                        <button data-variant="danger" data-size="sm"
                                data-attr:disabled="${{ import_running.id() }}"
                                data-on:click="@post('{{ forceRescan.url() }}')">
                            Confirm &amp; Rescan
                        </button>
                        <button data-size="sm"
                                data-on:click="${{ confirm_rescan.id() }} = false">
                            Cancel
                        </button>
                    </div>
                </div>
                {% endif %}

                {# Cancel — only visible while an import is running #}
                <div class="cluster">
                    <button data-variant="primary"
                            data-show="${{ import_running.id() }}" style="display: none;"
                            data-indicator:_cancel_loading
                            data-on:click="@post('{{ cancelImport.url() }}')">
                        <span data-show="$_cancel_loading" style="display: none;" class="spinner" role="status" aria-hidden="true"></span>
                        <svg data-show="!$_cancel_loading" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"  viewBox="0 0 16 16">
                            <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14m0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16"/>
                            <path d="M4.646 4.646a.5.5 0 0 1 .708 0L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 5.354a.5.5 0 0 1 0-.708"/>
                        </svg>
                        Cancel Import
                    </button>

                    {# Port-scanning toggle — only shown when ports are configured in settings #}
                    {% if hasPorts %}
                    <div class="choice">
                        <input type="checkbox"  id="adminScanPorts"
                               {{ bind(import_scan_ports) }}
                               data-attr:disabled="${{ import_running.id() }}">
                        <label class="muted" for="adminScanPorts">Scan ports</label>
                    </div>
                    {% endif %}
                </div>

            {% else %}

                {# ── Single-profile action buttons ───────────────────────── #}
                <div class="cluster">

                <button data-variant="primary"
                        data-attr:disabled="${{ import_running.id() }}"
                        data-show="!${{ import_running.id() }}"
                        data-on:click="@post('{{ triggerImport.url() }}')">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"  viewBox="0 0 16 16">
                        <path fill-rule="evenodd" d="M8 3a5 5 0 1 0 4.546 2.914.5.5 0 0 1 .908-.417A6 6 0 1 1 8 2z"/>
                        <path d="M8 4.466V.534a.25.25 0 0 1 .41-.192l2.36 1.966c.12.1.12.284 0 .384L8.41 4.658A.25.25 0 0 1 8 4.466"/>
                    </svg>
                    Trigger Import
                </button>

                {% if datasourceAcceptsHistoricWrites %}
                <button 
                        data-attr:disabled="${{ import_running.id() }}"
                        data-show="!${{ import_running.id() }}"
                        title="Re-reads every capture file, including those older than the newest sample. Nothing is deleted."
                        data-on:click="@post('{{ backfillImport.url() }}')">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"  viewBox="0 0 16 16">
                        <path fill-rule="evenodd" d="M8 3a5 5 0 1 1-4.546 2.914.5.5 0 0 0-.908-.417A6 6 0 1 0 8 2z"/>
                        <path d="M8 4.466V.534a.25.25 0 0 0-.41-.192L5.23 2.308c-.12.1-.12.284 0 .384l2.36 1.966A.25.25 0 0 0 8 4.466"/>
                    </svg>
                    Backfill
                </button>
                {% else %}
                <button class="kill"
                        data-attr:disabled="${{ import_running.id() }}"
                        data-show="!${{ confirm_rescan.id() }} && !${{ import_running.id() }}"
                        data-on:click="${{ confirm_rescan.id() }} = true">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"  viewBox="0 0 16 16">
                        <path fill-rule="evenodd" d="M8 3a5 5 0 1 1-4.546 2.914.5.5 0 0 0-.908-.417A6 6 0 1 0 8 2z"/>
                        <path d="M8 4.466V.534a.25.25 0 0 0-.41-.192L5.23 2.308c-.12.1-.12.284 0 .384l2.36 1.966A.25.25 0 0 0 8 4.466"/>
                    </svg>
                    Force Rescan
                </button>
                {% endif %}

                <button data-variant="primary"
                        data-show="${{ import_running.id() }}" style="display: none;"
                        data-indicator:_cancel_loading
                        data-on:click="@post('{{ cancelImport.url() }}')">
                    <span data-show="$_cancel_loading" style="display: none;" class="spinner" role="status" aria-hidden="true"></span>
                    <svg data-show="!$_cancel_loading" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"  viewBox="0 0 16 16">
                        <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14m0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16"/>
                        <path d="M4.646 4.646a.5.5 0 0 1 .708 0L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 5.354a.5.5 0 0 1 0-.708"/>
                    </svg>
                    Cancel Import
                </button>

                {% if not datasourceAcceptsHistoricWrites %}
                <div data-show="${{ confirm_rescan.id() }}" style="display: none;">
                    <div class="cluster">
                    <span class="text-danger strong muted">
                        ⚠ Resets all {{ deployDatasource }} data and re-imports from scratch.
                    </span>
                    <button data-variant="danger" data-size="sm"
                            data-attr:disabled="${{ import_running.id() }}"
                            data-on:click="@post('{{ forceRescan.url() }}')">
                        Confirm &amp; Rescan
                    </button>
                    <button data-size="sm"
                            data-on:click="${{ confirm_rescan.id() }} = false">
                        Cancel
                    </button>
                    </div>
                </div>
                {% endif %}

                {# Port-scanning toggle — only shown when ports are configured in settings #}
                {% if hasPorts %}
                <div class="choice">
                    <input type="checkbox"  id="adminScanPorts"
                           {{ bind(import_scan_ports) }}
                           data-attr:disabled="${{ import_running.id() }}">
                    <label class="muted" for="adminScanPorts">Scan ports</label>
                </div>
                {% endif %}

                </div>

            {% endif %}

            </div>{# /action buttons #}

            {# ── Progress bar — visible while import is running ────────────── #}
            <div data-show="${{ import_running.id() }}" style="display: none;" >
                <div class="cluster cluster-between">
                    <small class="muted">{{ importStatusText }}</small>
                    <small class="strong">{{ importProgress }}%{% if importEta %} &mdash; {{ importEta }}{% endif %}</small>
                </div>
                <progress class="import-progress" max="100" value="{{ importProgress }}">{{ importProgress }}%</progress>
                <div class="muted{{ importCurrentFile ? '' : ' invisible' }}">
                    Current: <code>{{ importCurrentFile ?: '…' }}</code>
                </div>
            </div>

            {# ── Import status — shown after completion ─────────────────────── #}
            {% if importStatusText and not import_running.getValue() %}
            <div>
                <div class="notice muted" data-level="{{ importLog|filter(e => e.level <= 3)|length > 0 ? 'error' : (importLog|length > 0 ? 'warning' : 'success') }}">
                    {{ importStatusText }}
                </div>
            </div>
            {% endif %}

            {# ── Import log ────────────────────────────────────────────────── #}
            {% if importLog|length > 0 %}
            <div>
                <div class="card">
                    <div class="card-header muted strong text-warning" data-level="warning">
                        Import log ({{ importLog|length }} warning{{ importLog|length != 1 ? 's' : '' }})
                    </div>
                    <div class="card-body">
                        <div  style="max-height: 240px;">
                            <table class="mono muted">
                                <tbody>
                                {% for entry in importLog|reverse %}
                                    <tr data-level="{{ entry.level <= 3 ? 'error' : 'warning' }}">
                                        <td class="muted" style="width:1%"><span data-text="new Date({{ entry.ts }} * 1000).toLocaleString('sv', {timeStyle:'medium'})">{{ entry.ts|date('H:i:s') }}</span></td>
                                        <td>{{ entry.msg }}</td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
            {% endif %}

        </div>{# /import section #}

        {# ── Health section ───────────────────────────────────────────────── #}
        <div data-show="$_settingsSection == 'health'" style="display:none;">

            {% set hcErrors = healthChecks|filter(c => c.status == 'error')|length %}
            {% set hcWarnings = healthChecks|filter(c => c.status == 'warning')|length %}
            <div>
                <div class="card">
                    <div class="cluster card-header strong cluster-between">
                        System Health
                        <span>
                            {% if hcErrors > 0 %}
                                <span class="badge" data-level="error">{{ hcErrors }} error{{ hcErrors != 1 ? 's' : '' }}</span>
                            {% elseif hcWarnings > 0 %}
                                <span class="badge text-warning" data-level="warning">{{ hcWarnings }} warning{{ hcWarnings != 1 ? 's' : '' }}</span>
                            {% else %}
                                <span class="badge" data-level="success">All checks passed</span>
                            {% endif %}
                        </span>
                    </div>
                    <div class="card-body">
                        <table>
                            <tbody>
                                {% for check in healthChecks %}
                                {% if loop.first or check.group != healthChecks[loop.index0 - 1].group %}
                                <tr aria-selected="true">
                                    <td colspan="2" class="muted strong upper" style="letter-spacing:.04em; font-size:.7rem">{{ check.group }}</td>
                                </tr>
                                {% endif %}
                                <tr data-level="{{ check.status == 'error' ? 'error' : (check.status == 'warning' ? 'warning' : '') }}">
                                    <td  style="width:40%">
                                        <span class="text-{{ check.status == 'error' ? 'danger' : (check.status == 'warning' ? 'warning' : 'success') }}">●</span>
                                        {{ check.label }}
                                    </td>
                                    <td class="muted">
                                        {% if check.code %}<code>{{ check.detail }}</code>{% else %}{{ check.detail|raw }}{% endif %}
                                        {% if check.epoch > 0 %}
                                            <br><span class="mono" style="font-size:.85em" data-text="new Date({{ check.epoch }} * 1000).toLocaleString(undefined, tzOptions(${{ displayTz.id() }}, ${{ nfcapdTz.id() }}))">{{ check.epoch|date('Y-m-d H:i:s') }}</span>
                                        {% endif %}
                                        {% if check.hint %}
                                            <br><small class="muted" style="opacity:.65; font-size:.78em">{{ check.hint }}</small>
                                        {% endif %}
                                    </td>
                                </tr>
                                {% else %}
                                <tr>
                                    <td colspan="2" class="muted italic">No checks available.</td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

        </div>{# /health section #}

        {# ── Preferences section (default) ────────────────────────────────── #}
        <div data-show="$_settingsSection == 'preferences'">

            <div class="card">
                <div class="card-header strong">User Preferences</div>
                <div class="card-body">
                    <p class="muted">
                        These settings are saved to <code>preferences.json</code> and applied on top of
                        your deployment configuration. They take effect immediately across all open tabs.
                    </p>

                    <div>

                        {# Default view #}
                        <div>
                            <label class="strong muted">Default view</label>
                            {% set _v = settings_defaultView.string() %}
                            <select  {{ bind(settings_defaultView) }}>
                                <option value="graphs" {{ _v == 'graphs' ? 'selected' : '' }}>Graphs</option>
                                <option value="flows" {{ _v == 'flows' ? 'selected' : '' }}>Flows</option>
                                <option value="statistics" {{ _v == 'statistics' ? 'selected' : '' }}>Statistics</option>
                            </select>
                        </div>

                        {# Default log level #}
                        <div>
                            <label class="strong muted">Log level</label>
                            {% set _v = settings_logPriority.string() %}
                            <select  {{ bind(settings_logPriority) }}>
                                <option value="debug" {{ _v == 'debug' ? 'selected' : '' }}>Debug</option>
                                <option value="info" {{ _v == 'info' ? 'selected' : '' }}>Info</option>
                                <option value="notice" {{ _v == 'notice' ? 'selected' : '' }}>Notice</option>
                                <option value="warning" {{ _v == 'warning' ? 'selected' : '' }}>Warning</option>
                                <option value="error" {{ _v == 'error' ? 'selected' : '' }}>Error</option>
                            </select>
                        </div>

                    </div>{# /row #}

                    <hr>
                    <h6 class="muted upper">Graph defaults</h6>

                    <div>

                        {# Default graph display #}
                        <div>
                            <label class="strong muted">Display</label>
                            {% set _v = settings_graphDisplay.string() %}
                            <select  {{ bind(settings_graphDisplay) }}>
                                <option value="sources" {{ _v == 'sources' ? 'selected' : '' }}>Sources</option>
                                <option value="ports" {{ _v == 'ports' ? 'selected' : '' }}>Ports</option>
                                <option value="protocols" {{ _v == 'protocols' ? 'selected' : '' }}>Protocols</option>
                            </select>
                        </div>

                        {# Default graph datatype #}
                        <div>
                            <label class="strong muted">Datatype</label>
                            {% set _v = settings_graphDatatype.string() %}
                            <select  {{ bind(settings_graphDatatype) }}>
                                <option value="traffic" {{ _v == 'traffic' ? 'selected' : '' }}>Traffic (bits/s)</option>
                                <option value="packets" {{ _v == 'packets' ? 'selected' : '' }}>Packets/s</option>
                                <option value="bytes" {{ _v == 'bytes' ? 'selected' : '' }}>Bytes</option>
                                <option value="flows" {{ _v == 'flows' ? 'selected' : '' }}>Flows</option>
                            </select>
                        </div>

                        {# Default graph protocols #}
                        <div>
                            <label class="strong muted">Protocols</label>
                            {% set _protos = settings_graphProtocols.array() %}
                            <select  {{ bind(settings_graphProtocols) }} multiple>
                                <option value="any" {{ 'any' in _protos ? 'selected' : '' }}>Any</option>
                                <option value="tcp" {{ 'tcp' in _protos ? 'selected' : '' }}>TCP</option>
                                <option value="udp" {{ 'udp' in _protos ? 'selected' : '' }}>UDP</option>
                                <option value="icmp" {{ 'icmp' in _protos ? 'selected' : '' }}>ICMP</option>
                                <option value="other" {{ 'other' in _protos ? 'selected' : '' }}>Other</option>
                            </select>
                            <div class="help">Hold Ctrl/⌘ to select multiple.</div>
                        </div>

                    </div>{# /row #}

                    <hr>
                    <h6 class="muted upper">Flow &amp; statistics defaults</h6>

                    <div>

                        {# Default flow limit #}
                        <div>
                            <label class="strong muted">Flow limit</label>
                            {% set _v = settings_flowLimit.int() %}
                            <select  {{ bind(settings_flowLimit) }}>
                                {% for n in [20, 50, 100, 500, 1000, 10000] %}
                                <option value="{{ n }}" {{ _v == n ? 'selected' : '' }}>{{ n }}</option>
                                {% endfor %}
                            </select>
                        </div>

                        {# Default stats order-by #}
                        <div>
                            <label class="strong muted">Statistics order by</label>
                            {% set _v = settings_statsOrderBy.string() %}
                            <select  {{ bind(settings_statsOrderBy) }}>
                                <option value="bytes" {{ _v == 'bytes' ? 'selected' : '' }}>Bytes</option>
                                <option value="packets" {{ _v == 'packets' ? 'selected' : '' }}>Packets</option>
                                <option value="flows" {{ _v == 'flows' ? 'selected' : '' }}>Flows</option>
                                <option value="pps" {{ _v == 'pps' ? 'selected' : '' }}>Packets/s</option>
                                <option value="bps" {{ _v == 'bps' ? 'selected' : '' }}>Bits/s</option>
                                <option value="bpp" {{ _v == 'bpp' ? 'selected' : '' }}>Bytes/packet</option>
                            </select>
                        </div>

                    </div>{# /row #}

                    <hr>
                    <h6 class="muted upper">Date &amp; time display</h6>

                    <div>
                        <div>
                            <label class="strong muted">Timezone</label>
                            <div class="help">Choose which timezone to use when displaying dates in charts and timestamps.</div>
                            <div class="choice">
                                <input  type="radio" name="displayTz" id="displayTz_browser"
                                       value="browser"
                                       {{ bind(displayTz) }}>
                                <label  for="displayTz_browser">
                                    Browser timezone <span class="muted">(your local time)</span>
                                </label>
                            </div>
                            <div class="choice">
                                <input  type="radio" name="displayTz" id="displayTz_server"
                                       value="server"
                                       {{ bind(displayTz) }}>
                                <label  for="displayTz_server">
                                    Capture timezone (<span data-text="${{ nfcapdTz.id() }}"></span>)
                                </label>
                            </div>
                        </div>
                    </div>{# /row #}

                    <hr>
                    <h6 class="muted upper">Filter presets</h6>

                    <div>
                        <div>
                            <label class="strong muted">
                                nfdump filter presets
                                <span class="muted">(one per line — available as quick-select in the Flows and Statistics tabs)</span>
                            </label>
                            <textarea id="settings-filters-textarea"
                                      class="mono"
                                      {{ bind(settings_filtersText) }}
                                      rows="5"
                                      placeholder="proto tcp&#10;dst port 443&#10;host 192.168.1.1"></textarea>
                        </div>
                    </div>{# /row #}

                    <div class="cluster cluster-end">
                        <button data-variant="primary" data-size="sm"
                                data-indicator:_settings_saving
                                data-on:click="document.dispatchEvent(new CustomEvent('nfsen-global-filters-updated',{detail:{filters:(document.getElementById('settings-filters-textarea')?.value||'').split('\n').map(s=>s.trim()).filter(s=>s)}})); window.showMessage('success', 'Preferences saved.', true); @post('{{ saveSettings.url() }}')">
                            <span data-show="$_settings_saving" style="display:none;"
                                  class="spinner" role="status" aria-hidden="true"></span>
                            Save preferences
                        </button>
                    </div>

                </div>{# /card-body #}
            </div>{# /card #}

        </div>{# /preferences section #}

        {# ── Alerts section ───────────────────────────────────────────────── #}
        <div data-show="$_settingsSection == 'alerts'" style="display:none;" >
            {% include 'partials/alerts-settings.html.twig' %}
        </div>

        {# ── System section ───────────────────────────────────────────────── #}
        <div data-show="$_settingsSection == 'system'" style="display:none;">

            <div class="card">
                <div class="cluster card-header strong cluster-between">
                    Deployment configuration
                    <span class="badge">read-only</span>
                </div>
                <div class="card-body">
                    <p class="muted">
                        These values come from environment variables or <code>settings.php</code>.
                        Restart the container to apply changes.
                    </p>
                    <table class="muted">
                        <tbody>
                            <tr>
                                <td class="muted" style="width:40%">Sources</td>
                                <td><code>{{ sources|join(', ') ?: '(none configured)' }}</code></td>
                            </tr>
                            <tr>
                                <td class="muted">Ports</td>
                                <td><code>{{ ports|join(', ') ?: '(none configured)' }}</code></td>
                            </tr>
                            <tr>
                                <td class="muted">Datasource</td>
                                <td><code>{{ deployDatasource }}</code></td>
                            </tr>
                            <tr>
                                <td class="muted">Import years</td>
                                <td><code>{{ deployImportYears }}</code></td>
                            </tr>
                            <tr>
                                <td class="muted">Default theme</td>
                                <td><code>{{ deployDefaultTheme }}</code>{% if deployDefaultTheme == 'auto' %} <span class="muted">(follows browser)</span>{% endif %}</td>
                            </tr>
                            <tr>
                                <td class="muted">nfdump binary</td>
                                <td><code>{{ deployNfdumpBinary }}</code></td>
                            </tr>
                            <tr>
                                <td class="muted">Profiles data path</td>
                                <td><code>{{ deployNfdumpProfiles }}</code></td>
                            </tr>
                            <tr>
                                <td class="muted">Preferences file</td>
                                <td><code>{{ deployPrefsFile }}</code></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

        </div>{# /system section #}

    </div>{# /content panels #}

</div>{# /row #}
